"use client"
export default function Home() {
  return (
      <section>
        <h1>Hello Word</h1>
        <button onClick={() => {

          alert('hola');
        }}>El boton</button>
      </section>

  );
}
